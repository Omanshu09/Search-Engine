# Copy to .env (or .env.production) for local prod-mode testing.
# In Vercel/Netlify, set this as a project environment variable instead --
# it must point at your deployed backend's public URL, with /api at the end.
VITE_API_BASE_URL=https://your-backend-host.example.com/api
